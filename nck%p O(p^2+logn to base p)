#include<bits/stdc++.h>
using namespace std;
typedef long long lli;
const lli m=11;
vector<lli> *arr;
void nck(lli n)
{
  
  for(lli x=0;x<=n;x++)
  for(lli y=0;y<=x;y++)
  arr[x].push_back(0);
  for(lli i=0;i<=n;i++)
  {
    arr[i][0]=1;
    arr[i][i]=1;
  }
  for(lli i=1;i<=n;i++)
  for(lli j=1;j<i;j++)
  arr[i][j]=(arr[i-1][j-1]+arr[i-1][j])%lli(m);
}
lli compute(lli n,lli k)
{
    if(n<k)
    return 0;
    if(n==0||k==0)
    return 1;
    lli x,y;
    x=n/m;
    y=k/m;
    return (compute(x,y)%m*arr[n%m][k%m]%m)%m;
}
int main()
{
    arr=new vector<lli>[m+1];
    nck(m);
    cout<<compute(100,99);
    delete []arr;
    return 0;
}
